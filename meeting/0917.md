# 09/17 會議紀錄

## 會議結論

### 1. 試用 O2IMS 進行連接

- 決定試用 O2IMS 進行連接。
- 撰寫對應的 PM Mapping。

### 2. 確認測試方式

- 評估以下兩種測試方式，最終採用方式待確認：
  - 使用 `stress-ng --cpu 4 --timeout 60s` 產生 CPU 負載。
  - 使用 Ming’s e2e 輸入不同的 iperf 測試條件。

### 3. 蒐集 CortexDC CPU 使用率

- 在伺服器上安裝 Node Exporter，以蒐集 CPU 使用率相關指標。
- 預定於 09/24 前完成 Node Exporter 安裝，蒐集 CPU usage 並寫入 InfluxDB。

### 4. 設計實驗圖表

- X 軸：輸入的流量大小。
- Y 軸：各個 Feature 的量測值。
- 透過圖表觀察不同流量對各個 Feature 的影響。

## 待補資訊

- 其餘工作的完成期限。
- 最終採用的測試方式。
- 實驗流量設定與 Feature 的具體項目。
